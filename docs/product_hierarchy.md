# Product Hierarchy

> **Work in progress.** This document will specify the full processing
> hierarchy (L0 → L1A → L1B → L1C → L2), with inputs, outputs, processing steps
> and physical justification for each level. It is deliverable #1 of the
> challenge and will be completed once the L1B implementation is in place so the
> specification reflects the real data and calibration model.

For the implemented transition (L0/L1A → **L1B TOA radiance**) see the code under
[`src/spp/`](../src/spp/) and the architecture overview in
[`architecture.md`](architecture.md).
